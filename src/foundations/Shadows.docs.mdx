import { Meta, Stories } from '@storybook/addon-docs/blocks';
import * as ShadowsStories from './Shadows.stories';

<Meta of={ShadowsStories} />

# Shadows

The elevation scale, from `shadow-z1` to `shadow-z5`, generated from the
`--shadow-*` theme namespace in `styles/tokens.css`. `z4` is verified
against Figma; the rest are interpolated between it and the neighboring
steps.

<Stories />
