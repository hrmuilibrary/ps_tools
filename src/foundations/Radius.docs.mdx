import { Meta, Stories } from '@storybook/addon-docs/blocks';
import * as RadiusStories from './Radius.stories';

<Meta of={RadiusStories} />

# Radius

The border-radius scale, from `rounded-xs` (2px) to `rounded-full`. Tailwind
v4 maps the `--radius-*` theme namespace directly to `rounded-*` utilities,
so these are the exact classes components use — not a separate token layer.

<Stories />
